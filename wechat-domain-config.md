# 微信小程序服务器域名配置指南

## 📋 需要配置的域名列表

根据你的项目代码，需要配置以下域名：

### 一、request合法域名（HTTPS）

```
1. https://ark.cn-beijing.volces.com
   ↑ 火山引擎豆包 LLM API

2. https://open.bigmodel.cn
   ↑ 智谱AI ASR API

3. https://openspeech.bytedance.com
   ↑ 字节跳动语音服务（ASR + TTS）
```

### 二、socket合法域名（WSS）

```
wss://openspeech.bytedance.com
   ↑ 豆包实时语音对话 WebSocket
```

---

## 🔧 详细配置步骤

### 第一步：登录微信小程序后台

1. 打开浏览器，访问：https://mp.weixin.qq.com/
2. 使用小程序管理员账号扫码登录
3. 进入你的小程序管理后台

### 第二步：配置服务器域名

1. **进入开发设置**
   - 点击左侧菜单"开发"
   - 点击"开发设置"

2. **配置 request 合法域名**
   - 找到"服务器域名"→"request合法域名"
   - 点击"修改"按钮
   - 依次添加以下3个域名：

```
ark.cn-beijing.volces.com
open.bigmodel.cn
openspeech.bytedance.com
```

   - 每次输入一个域名后点击"保存"
   - 注意：**不要**带 `https://` 前缀
   - 注意：**不要**带路径（如 `/api/v3`）

3. **配置 socket 合法域名**
   - 找到"socket合法域名"
   - 点击"修改"按钮
   - 添加以下域名：

```
openspeech.bytedance.com
```

   - 同样不要带 `wss://` 前缀

4. **点击保存**

### 第三步：检查配置状态

保存后，域名列表应该显示为：

**request 合法域名：**
- ✅ ark.cn-beijing.volces.com
- ✅ open.bigmodel.cn
- ✅ openspeech.bytedance.com

**socket 合法域名：**
- ✅ openspeech.bytedance.com

---

## 📝 域名说明

### 1. 豆包 LLM API
- **域名**：ark.cn-beijing.volces.com
- **用途**：AI 对话生成
- **提供方**：火山引擎（字节跳动旗下）

### 2. 智谱 ASR API
- **域名**：open.bigmodel.cn
- **用途**：语音识别（备选方案）
- **提供方**：智谱AI

### 3. 字节语音服务
- **域名**：openspeech.bytedance.com
- **用途**：
  - 语音识别（ASR）
  - 语音合成（TTS）
  - 实时语音对话（WebSocket）
- **提供方**：字节跳动

---

## ⚠️ 重要提示

### 域名配置注意事项

1. **不要带协议前缀**
   - ❌ 错误：`https://openspeech.bytedance.com`
   - ✅ 正确：`openspeech.bytedance.com`

2. **不要带路径**
   - ❌ 错误：`openspeech.bytedance.com/api/v1`
   - ✅ 正确：`openspeech.bytedance.com`

3. **必须是HTTPS**
   - 微信小程序只允许HTTPS协议
   - 所有API都使用HTTPS

4. **端口号**
   - 如果有端口号，需要明确配置
   - 本项目所有API都使用默认443端口，无需额外配置

### WebSocket 配置注意事项

1. **必须配置socket合法域名**
   - 实时语音对话使用WebSocket连接
   - 如果不配置，实时语音功能无法使用

2. **域名格式**
   - 配置时不带 `wss://` 前缀
   - 只配置域名部分

---

## 🧪 测试步骤

配置完成后，建议按以下步骤测试：

### 测试1：基础AI对话
1. 打开小程序
2. 输入问题："天空为什么是蓝色的？"
3. 检查是否正常返回答案

### 测试2：语音识别
1. 点击语音模式
2. 点击"按住说话"
3. 说出问题
4. 松开后检查文字识别是否正确

### 测试3：语音合成
1. AI返回答案后
2. 检查是否自动播放语音
3. 语音是否清晰

### 测试4：实时语音对话
1. 切换到"实时语音模式"
2. 长按说话按钮
3. 实时对话是否流畅
4. 检查延迟和识别准确率

---

## 🔍 故障排查

### 问题1：request不在以下合法域名列表中

**原因**：域名未配置或配置错误

**解决方法**：
1. 检查域名是否已添加到"request合法域名"
2. 检查域名拼写是否正确
3. 确认没有带 `https://` 前缀
4. 清除缓存重新编译

### 问题2：WebSocket 连接失败

**原因**：socket域名未配置

**解决方法**：
1. 检查是否配置了"socket合法域名"
2. 确认配置的是 `openspeech.bytedance.com`
3. 重启小程序测试

### 问题3：SSL证书错误

**原因**：域名的SSL证书有问题

**解决方法**：
1. 确认API服务商的SSL证书有效
2. 尝试在小程序后台配置"不校验合法域名"（仅开发测试用）

### 问题4：部分功能可用，部分不可用

**原因**：只配置了部分域名

**解决方法**：
1. 对照域名列表检查是否全部配置
2. 特别是 `open.bigmodel.cn` 容易遗漏

---

## 📱 配置不校验合法域名（开发阶段）

**⚠️ 注意：仅用于开发测试，正式发布前必须配置正确域名**

在开发阶段，为了快速测试，可以临时关闭域名校验：

### 在微信开发者工具中：

1. 点击右上角"详情"
2. 找到"本地设置"
3. 勾选"不校验合法域名、web-view（业务域名）、TLS 版本以及 HTTPS 证书"

### 在真机预览时：

需要在微信小程序后台操作：
1. "开发"→"开发设置"→"开发版不校验域名"
2. 开启此选项（仅限开发阶段使用）

---

## ✅ 配置完成检查清单

- [ ] 所有3个request域名已配置
- [ ] socket域名已配置
- [ ] 域名拼写正确（无https://前缀）
- [ ] 域名无路径（纯域名）
- [ ] 已保存配置
- [ ] 清除缓存重新编译
- [ ] 在开发者工具中测试通过
- [ ] 在真机中测试通过

---

## 📞 需要帮助？

如果在配置过程中遇到问题，可以：

1. **查看官方文档**
   - 微信小程序开发文档：https://developers.weixin.qq.com/miniprogram/dev/framework/server-ability/domain.html

2. **联系API服务商**
   - 火山引擎技术支持
   - 智谱AI技术支持
   - 字节跳动技术支持

3. **查看项目配置**
   - 检查 `.env` 文件中的API端点
   - 检查 `src/config/env.ts` 中的配置

---

**配置完成后**，建议：
1. 先在微信开发者工具中测试
2. 再使用"真机预览"功能测试
3. 确认所有功能正常后再上传代码
4. 提交审核前务必再次检查所有功能

**更新时间**：2026-03-27
**项目版本**：v1.0
